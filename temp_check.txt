<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ book.metadata.title }}</title>
    <style>
        :root {
            --bg-color: #fff;
            --text-color: #212529;
            --sidebar-bg: #f8f9fa;
            --sidebar-border: #e9ecef;
            --sidebar-text: #495057;
            --link-color: #3498db;
            --link-hover: #000;
            --active-link: #d63384;
            --heading-color: #333;
            --nav-border: #eee;
            --btn-border: #3498db;
            --btn-hover-bg: #3498db;
            --btn-hover-text: white;
            --disabled-color: #ccc;
            
            /* --- WARNA HIGHLIGHT --- */
            --highlight-bg: #ffeb3b; 
            --highlight-border: #fbc02d;
        }

        body.dark-mode {
            --bg-color: #121212;
            --text-color: #d0d0d0;
            --sidebar-bg: #1e1e1e;
            --sidebar-border: #333;
            --sidebar-text: #b0b0b0;
            --link-color: #4fa3d1;
            --link-hover: #fff;
            --active-link: #ff79c6;
            --heading-color: #fff;
            --nav-border: #333;
            --btn-border: #4fa3d1;
            --btn-hover-bg: #4fa3d1;
            --btn-hover-text: #121212;
            --disabled-color: #555;

            /* --- WARNA HIGHLIGHT DARK MODE --- */
            --highlight-bg: #6c6404; 
            --highlight-border: #827717;
        }

        /* =========================================
           SCROLLBAR STYLING (Diambil dari request Anda)
           ========================================= */
        
        /* Sidebar Scrollbar */
        #sidebar::-webkit-scrollbar {
            width: 8px;
        }
        #sidebar::-webkit-scrollbar-track {
            background: var(--sidebar-bg);
        }
        #sidebar::-webkit-scrollbar-thumb {
            background: var(--sidebar-border);
            border-radius: 4px;
        }
        #sidebar::-webkit-scrollbar-thumb:hover {
            background: var(--sidebar-text);
        }

        /* Main Content Scrollbar */
        #main::-webkit-scrollbar {
            width: 10px;
        }
        #main::-webkit-scrollbar-track {
            background: var(--bg-color);
        }
        #main::-webkit-scrollbar-thumb {
            background: var(--btn-border); /* Warna Biru */
            border-radius: 5px;
        }
        #main::-webkit-scrollbar-thumb:hover {
            background: var(--link-color);
        }

        /* =========================================
           LAYOUT & BASIC STYLES
           ========================================= */

        body { margin: 0; padding: 0; height: 100vh; overflow: hidden; font-family: "Georgia", serif; background: var(--bg-color); color: var(--text-color); transition: background 0.3s, color 0.3s; }

        /* Sidebar Layout */
        #sidebar { width: 300px; background: var(--sidebar-bg); border-right: 1px solid var(--sidebar-border); overflow-y: auto; padding: 20px; flex-shrink: 0; position: fixed; left: 0; top: 0; bottom: 0; transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1); z-index: 10; }
        body.sidebar-hidden #sidebar { transform: translateX(-100%); }
        .nav-header { font-family: -apple-system, sans-serif; font-weight: bold; color: var(--sidebar-text); margin-bottom: 15px; padding-bottom: 10px; border-bottom: 1px solid var(--sidebar-border); }
        .nav-home { display: block; margin-bottom: 10px; color: var(--link-color); text-decoration: none; font-size: 0.9em; }

        /* Toggle Button */
        .sidebar-toggle-btn {
            position: fixed; top: 20px; left: 360px; z-index: 1000;
            background: var(--bg-color); border: 1px solid var(--btn-border);
            color: var(--link-color); padding: 8px 12px; border-radius: 4px;
            cursor: pointer; font-family: -apple-system, sans-serif;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            box-shadow: 0 2px 8px rgba(0,0,0,0.15);
        }
        body.sidebar-hidden .sidebar-toggle-btn { left: 20px; }
        .sidebar-toggle-btn:hover { background: var(--btn-hover-bg); color: var(--btn-hover-text); transform: scale(1.05); }

        /* TOC */
        ul.toc-list { list-style: none; padding-left: 0; margin: 0; }
        ul.toc-list ul { padding-left: 20px; }
        li.toc-item { margin-bottom: 8px; }
        a.toc-link { text-decoration: none; color: var(--sidebar-text); font-size: 0.95em; display: block; padding: 4px 0; line-height: 1.4; }
        a.toc-link:hover { color: var(--link-hover); text-decoration: underline; }
        a.toc-link.active { color: var(--active-link); font-weight: bold; }

        /* Main Content Layout */
        #main { position: fixed; top: 0; bottom: 0; right: 0; overflow-y: auto; scroll-behavior: smooth; margin-left: 300px; transition: margin-left 0.4s cubic-bezier(0.4, 0, 0.2, 1); width: calc(100% - 300px); }
        body.sidebar-hidden #main { margin-left: 0; width: 100%; }
        .content-container { max-width: 700px; margin: 0 auto; padding: 60px 40px; line-height: 1.8; font-size: 1.15em; }
        .book-content img { max-width: 100%; height: auto; display: block; margin: 20px auto; }
        .book-content h1, .book-content h2 { font-family: -apple-system, sans-serif; margin-top: 1.5em; color: var(--heading-color); }
        .book-content p { margin-bottom: 1.5em; text-align: justify; }

        /* Footer Nav */
        .chapter-nav { display: flex; justify-content: space-between; margin-top: 60px; padding-top: 20px; border-top: 1px solid var(--nav-border); font-family: -apple-system, sans-serif; }
        .nav-btn { text-decoration: none; color: var(--link-color); font-weight: bold; padding: 10px 20px; border: 1px solid var(--btn-border); border-radius: 4px; }
        .nav-btn:hover { background: var(--btn-hover-bg); color: var(--btn-hover-text); }
        .nav-btn.disabled { opacity: 0.5; pointer-events: none; border-color: var(--disabled-color); color: var(--disabled-color); }
        
        .theme-toggle { display: block; width: 100%; text-align: left; background: none; border: 1px solid var(--sidebar-border); color: var(--sidebar-text); padding: 8px; margin-bottom: 20px; border-radius: 4px; cursor: pointer; }

        /* HIGHLIGHT MARKER STYLE */
        .highlight-marker {
            background-color: var(--highlight-bg);
            border-bottom: 2px solid var(--highlight-border);
            color: inherit; cursor: pointer; border-radius: 2px; padding: 0 2px; transition: opacity 0.2s;
        }
        body.dark-mode .highlight-marker { color: #fff; }
        .highlight-marker:hover { opacity: 0.8; }

        /* --- POPUP STYLING --- */
        #translation-popup {
            position: absolute; display: none;
            background: var(--bg-color); border: 1px solid var(--btn-border);
            box-shadow: 0 4px 15px rgba(0,0,0,0.2);
            padding: 0; border-radius: 8px; z-index: 9999;
            min-width: 320px; max-width: 400px;
            font-family: -apple-system, sans-serif; font-size: 0.95em;
            overflow: hidden;
        }
        #translation-popup .popup-header {
            display: flex; align-items: center; gap: 8px;
            cursor: move; user-select: none;
            background: var(--sidebar-bg);
            padding: 10px 12px; border-bottom: 1px solid var(--sidebar-border);
        }
        #translation-popup .popup-header label { font-size: 0.85em; white-space: nowrap; cursor: move; }
        #translation-popup select {
            flex: 1; background: var(--bg-color); color: var(--text-color);
            border: 1px solid var(--sidebar-border); padding: 4px 8px; border-radius: 4px;
        }
        #translation-popup .popup-body { padding: 12px; }
        
        /* Popup Scrollbar (Specific Override) */
        #translation-popup .popup-content {
            margin-bottom: 10px; color: var(--text-color); line-height: 1.5;
            max-height: 200px; overflow-y: auto; padding-right: 5px;
            scrollbar-width: thin; scrollbar-color: var(--sidebar-border) var(--bg-color);
        }
        #translation-popup .popup-content::-webkit-scrollbar { width: 6px; }
        #translation-popup .popup-content::-webkit-scrollbar-track { background: transparent; }
        #translation-popup .popup-content::-webkit-scrollbar-thumb { background: var(--sidebar-border); border-radius: 3px; }

        /* TEXTAREA NOTE */
        #note-input {
            width: 100%; height: 80px; box-sizing: border-box;
            background: var(--sidebar-bg); color: var(--text-color);
            border: 1px solid var(--sidebar-border);
            border-radius: 4px; padding: 8px;
            margin-bottom: 10px; font-family: inherit; resize: vertical;
            display: none;
    <div id="sidebar">
        <a href="/" class="nav-home">â† Back to Library</a>
        <button class="theme-toggle" onclick="toggleTheme()">ðŸŒ“ Theme</button>
        <div class="nav-header">{{ book.metadata.title }}</div>
        {% macro render_toc(items) %}
            <ul class="toc-list">
            {% for item in items %}
                <li class="toc-item">
                    {% set is_active = current_chapter.href == item.file_href %}
                    <a href="#" onclick="findAndGo('{{ item.file_href }}')" class="toc-link {% if is_active %}active{% endif %}">{{ item.title }}</a>
                    {% if item.children %}{{ render_toc(item.children) }}{% endif %}
                </li>
            {% endfor %}
            </ul>
        {% endmacro %}
        {{ render_toc(book.toc) }}
    </div>

    <div id="main">
